namespace RecipeAPI.IngredientMatching
{
    /// <summary>
    /// Flat combine mode for structured API requests (maps to OR-of-AND segments).
    /// </summary>
    public enum IngredientCombineMode
    {
        All,
        Any
    }
}
